---
// Jenkins CD Pipeline for AKS Deployment
// Single Deploy stage for Kubernetes deployment

pipeline {
    agent any
    
    environment {
        AZURE_SUBSCRIPTION = 'your-azure-subscription'
        RESOURCE_GROUP = 'your-resource-group'
        AKS_CLUSTER = 'your-aks-cluster'
        CONTAINER_REGISTRY = 'your-acr.azurecr.io'
        IMAGE_NAME = 'your-app'
        NAMESPACE = 'default'
        PROJECT_NAME = "${env.JOB_NAME}"
        ARTIFACT_NAME = "${env.JOB_NAME}-drop"
    }
    
    stages {
        stage('Deploy Stage') {
            steps {
                script {
                    // Download artifacts from CI build
                    copyArtifacts(
                        projectName: "${PROJECT_NAME}-ci",
                        selector: lastSuccessful(),
                        target: 'artifacts/'
                    )
                    
                    // Deploy to AKS
                    withCredentials([azureServicePrincipal('azure-service-principal')]) {
                        sh '''
                            az login --service-principal -u $AZURE_CLIENT_ID -p $AZURE_CLIENT_SECRET --tenant $AZURE_TENANT_ID
                            az aks get-credentials --resource-group $RESOURCE_GROUP --name $AKS_CLUSTER
                            
                            docker build -t $CONTAINER_REGISTRY/$IMAGE_NAME:$BUILD_NUMBER artifacts/
                            docker push $CONTAINER_REGISTRY/$IMAGE_NAME:$BUILD_NUMBER
                            
                            kubectl set image deployment/$IMAGE_NAME $IMAGE_NAME=$CONTAINER_REGISTRY/$IMAGE_NAME:$BUILD_NUMBER -n $NAMESPACE
                            kubectl rollout status deployment/$IMAGE_NAME -n $NAMESPACE
                        '''
                    }
                }
            }
        }
    }
    
    post {
        always {
            cleanWs()
        }
    }
}